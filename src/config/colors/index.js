const Colors = {
    WhiteOpacity: (opacity) => `rgba(255, 255, 255, ${opacity})`,
    BlackOpacity: (opacity) => `rgba(0, 0, 0, ${opacity})`,
    GreenOpacity: (opacity) => `rgba(57, 181, 74, ${opacity})`,
    Transparent: "transparent",
    Black: "#000",
    White: "#fff",
    Green: "#39B54A",
}

export default Colors;